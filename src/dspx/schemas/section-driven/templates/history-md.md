# history

<!--
退場散文細節（可選）。每個 ## <id> 段對應 history.yaml 一筆退場「決策」，寫「為什麼放棄」：
為何丟、輸給誰、證據。標題＝乾淨的 `## <id>`（純 id，不帶標題、不夾破折號）。
讀者＝develop / factcheck 兩個 agent；draft 永不讀、publish 不投影。
整節退場不寫這裡（細節＝它被封存的資料夾）。docspec retire 會開好段、搬入原散文。
-->
